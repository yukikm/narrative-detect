# Demo evidence (local)

Run:

```bash
pip install -e .
narrative detect data/sample_posts.jsonl --out out/report.md
narrative ideas out/report.json --out out/ideas.md
```

Then attach or screenshot:
- `out/report.md`
- `out/ideas.md`

(If you host a Streamlit demo, paste the link here.)
